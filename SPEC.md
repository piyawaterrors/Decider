System Specification: The Divine Decider (ระบบตัดสินใจแทนฉันที)
1. Project Overview
ระบบเว็บแอปพลิเคชันหน้าเดียว (SPA) ที่ช่วยตัดสินใจเรื่องไร้สาระในชีวิตประจำวัน โดยเน้นความกวนประสาท (Sarcastic AI Personality) มีการใช้บริบทจริงของผู้ใช้ (เวลา, สถานที่, สภาพอากาศ) มาประกอบการด่า และมีช่องทางรับเงินโดเนทค่ากาแฟ

2. Technical Stack
Frontend: React.js (Vite)

Styling: Tailwind CSS (เน้นสไตล์ Neo-brutalism หรือสไตล์ปุ่มมีเงาหนาๆ)

Animation: Framer Motion (สำหรับเอฟเฟกต์การสุ่ม)

State Management: React Hooks (useState, useEffect)

API Integration: OpenWeatherMap API (สำหรับเช็คสภาพอากาศ)

3. Key Features & Logic
A. Category Selection (หน้าแรก)
แสดงการ์ดหมวดหมู่ต่างๆ เช่น:

"กินไรดี" (ฉบับออฟฟิศซินโดรม)

"ลาป่วยวันนี้ใช้มุกไหน" (เอาชีวิตรอดจากบอส)

"ทักไปหาเขาดีไหม" (โหมดเตือนสติ)

"วิ่งหรือนอน" (สุขภาพดี vs สุขภาพจิตดี)

B. The Context-Aware Engine (หัวใจของระบบ)
ระบบต้องเช็คข้อมูลจาก Browser ก่อนแสดงผลลัพธ์:

Time Logic: ถ้าสุ่มหลังเที่ยงคืน หรือสุ่มเรื่องงานในวันหยุด ต้องเด้งคำด่าพิเศษ

Weather Logic: ถ้าสุ่มให้ไปข้างนอกแต่ฝนตก ต้องทักท้วงผู้ใช้

Click Counting: เก็บจำนวนครั้งที่ผู้ใช้กดสุ่มใหม่ ถ้ากดซ้ำเกิน 5 ครั้ง ให้ระบบ "ล็อกผลลัพธ์" และขึ้น QR Code เรียกค่าปรับ (ค่ากาแฟ)

C. Sarcastic UI/UX
Result Display: ผลลัพธ์ต้องแสดงตัวใหญ่ๆ พร้อมคำบรรยายใต้ภาพ (Insult Message) ที่สุ่มมาจากคลังคำด่า

Sound Effect: (Optional) เสียงตลกๆ เวลาสุ่ม หรือเสียงถอนหายใจ

4. Donation System (Monetization)
The Coffee Trigger: ปุ่ม "เลี้ยงกาแฟแอดมิน" ต้องเด่นชัด

Smart Pop-up: เมื่อผู้ใช้สุ่มได้ผลลัพธ์ที่ "ถูกใจ" หรือ "กวนประสาทเป็นพิเศษ" ให้แสดง Modal รูป QR Code PromptPay หรือลิงก์ Buy Me a Coffee พร้อมคำโปรย: "ค่าที่ปรึกษาแพงนะ เลี้ยงกาแฟหน่อย"

5. Directory Structure (Based on your Project)
src/data/categories.js: เก็บ Raw data ของเมนูและคำด่า

src/hooks/useDecision.js: จัดการ Logic การสุ่มและการเช็ค Context (Time, Weather, Count)

src/components/Decider/: คอมโพเนนต์แสดงหน้าสุ่ม

src/components/Donation/: คอมโพเนนต์ Modal QR Code

6. Target Output Example
User: กดสุ่ม "วิ่งหรือนอน" ตอนฝนตก 2 ทุ่ม System: "นอนไปเถอะ! ฝนตกขนาดนี้จะไปวิ่งเลียนแบบพระเอกเอ็มวีเหรอ? ถ้าอยากเสียเหงื่อก็โอนค่ากาแฟมานี่ แอดมินลุ้นจนเหงื่อตกแล้ว"

🛠 ส่วนเพิ่มเติมสเปค: การเชื่อมต่อ Supabase (The Connection Layer)
1. Configuration (Environment Variables):

ต้องมีไฟล์ .env ที่เก็บ VITE_SUPABASE_URL และ VITE_SUPABASE_ANON_KEY

สร้างไฟล์ src/services/supabaseClient.js เพื่อทำ Singleton Instance สำหรับเรียกใช้ทั่วทั้งแอป

2. Data Fetching Strategy:

Public Access: หน้าสุ่มผลลัพธ์ (Decider Page) ให้ดึงข้อมูลจากตาราง categories และ decisions_pool โดยใช้ supabase.from().select()

Real-time Updates: ใช้ supabase.channel() เพื่อทำระบบ Real-time Notification เมื่อมีคนโดเนทค่ากาแฟ หรือมีคนสุ่มได้ผลลัพธ์กวนๆ ให้แสดงผลบอกผู้ใช้อื่นทันที

3. Admin Security (RLS & Auth):

Admin-Only Access: หน้า /admin/* ต้องมีการตรวจสอบ Session ผ่าน supabase.auth.getSession()

Row Level Security (RLS): * ตาราง categories และ decisions_pool ตั้งค่าให้ SELECT ได้ทุกคน (Public)

ตาราง categories, decisions_pool, usage_logs ตั้งค่าให้ INSERT/UPDATE/DELETE ได้เฉพาะ Role authenticated (Admin) เท่านั้น

4. Integration Hooks:

สร้าง Custom Hook ชื่อ useSupabaseData ใน src/hooks/ เพื่อจัดการสถานะ Loading, Error และการดึงข้อมูลจาก DB มาแสดงที่ UI ของ shadcn